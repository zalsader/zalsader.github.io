<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://zalsader.com/feed.xml" rel="self" type="application/atom+xml"/><link href="https://zalsader.com/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-01-05T02:17:21+00:00</updated><id>https://zalsader.com/feed.xml</id><title type="html">blank</title><subtitle>Cloud Software engineer at &lt;a href=&apos;https://koor.tech/&apos;&gt;Koor Technologies, Inc.&lt;/a&gt;. </subtitle><entry><title type="html">Running Serverless AI with Knative and Ceph</title><link href="https://zalsader.com/blog/2024/ceph-ai/" rel="alternate" type="text/html" title="Running Serverless AI with Knative and Ceph"/><published>2024-03-05T21:02:00+00:00</published><updated>2024-03-05T21:02:00+00:00</updated><id>https://zalsader.com/blog/2024/ceph-ai</id><content type="html" xml:base="https://zalsader.com/blog/2024/ceph-ai/"><![CDATA[<p>This post was originally written while I worked at <a href="https://koor.tech">Koor Technologies</a>. Since that website is no longer online, I am republishing <a href="https://web.archive.org/web/20240416160909/https://blog.koor.tech/blog/2024/machine-learning-storage/">that article</a> here.</p> <div class="row mt-3 justify-content-center"> <div class="col-sm-6 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2024-03-05-ceph-ai/machine-learning-storage-480.webp 480w,/assets/img/2024-03-05-ceph-ai/machine-learning-storage-800.webp 800w,/assets/img/2024-03-05-ceph-ai/machine-learning-storage-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2024-03-05-ceph-ai/machine-learning-storage.jpg" class="img-fluid z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Machine Learning (ML) and Large Language Models (LLMs) are ubiquitous these days. They have applications ranging from generating images to writing articles (no, this article was written by a human 😉). There are many GenAI &amp; ML models available online free to use by tinkerers, but you would need to pay to incorporate them into your business logic. That’s why a wide range of <a href="https://www.pluralsight.com/resources/blog/cloud/what-is-machine-learning-as-a-service-mlaas">Machine Learning as a Service (MLaaS) providers</a> became available.</p> <p>But what if you wanted to escape the Big Cloud providers and build your own ML services? Whether you do this to save costs or to build internal processes, it could be worth it to consider deploying your ML models on Kubernetes. This tutorial builds on what we learned in <a href="/blog/2024/ceph-knative/">part 1</a> of this series, where we deployed a basic serverless application with storage, and demonstrates deploying a pre-trained ML model using Knative and Ceph. This tutorial is self-contained though, so if you haven’t read the first part, you can do that another time.</p> <p>As shown in the figure below, the pièce de résistance of our setup is the pre-trained ML model. We are using an open-source model from Hugging Face based on Stable diffusion called <a href="https://huggingface.co/timbrooks/instruct-pix2pix">instruct-pix2pix</a>. This model applies image editing prompts to an image. We added some more code around it to read the input image from a Ceph bucket and upload the output image to another Ceph bucket. We are also using Knative eventing to trigger the ML serverless function whenever an image is uploaded to the input Ceph bucket using the bucket notification feature in Ceph. You can replace the model with any other model that fits your needs to do more sophisticated image processing, generate accessible alt-texts, or dream up an image based on prompts. The possibilities are endless.</p> <div class="row mt-3 justify-content-center"> <div id="diagram" class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2024-03-05-ceph-ai/diagram-480.webp 480w,/assets/img/2024-03-05-ceph-ai/diagram-800.webp 800w,/assets/img/2024-03-05-ceph-ai/diagram-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2024-03-05-ceph-ai/diagram.png" class="img-fluid z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Using Ceph bucket notifications to trigger the pre-trained ML model </div> <p>All the code for this tutorial is <a href="https://github.com/koor-tech/demo-gitops/tree/b12e89953ea71beed97f1f8b182df6f7fd096a1d/exp/knative-ml">freely available on GitHub</a>.</p> <p>This tutorial is also available as a video:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <iframe src="https://www.youtube.com/embed/-rwmKgomQno" class="img-fluid z-depth-1 w-100" style="aspect-ratio: 16 / 9;" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen="" width="auto" height="auto"/> </figure> </div> </div> <h2 id="infrastructure-">Infrastructure 🏭</h2> <h3 id="prerequisites">Prerequisites</h3> <p>You need to have a working Kubernetes cluster that meets the minimum requirements of both <a href="https://rook.io/docs/rook/v1.13/Getting-Started/Prerequisites/prerequisites/">Rook</a> and <a href="https://knative.dev/docs/install/operator/knative-with-operators/#prerequisites">Knative</a>. This tutorial uses Rook v1.13.0 and Knative v1.12.0 . This is a summary of the requirements for a production cluster:</p> <ul> <li>Kubernetes v1.26 or newer.</li> <li>At least three nodes, each having at least 2 CPUs, 4 GB of memory, and 20 GB of disk storage.</li> <li><a href="https://rook.io/docs/rook/v1.13/Getting-Started/Prerequisites/prerequisites/#ceph-prerequisites">Raw devices</a> / partitions / logical volumes</li> <li>(Optional) Access to GPUs in the kubernetes cluster. This helps run the model faster, but you can run it without it.</li> </ul> <p>We ran this using our demo cluster, which we set up using <a href="https://www.terraform.io/">Terraform</a> and <a href="https://github.com/kubermatic/kubeone">Kubeone</a> on <a href="https://www.hetzner.com/cloud">Hetzner Cloud</a>.</p> <h3 id="rook-ceph">Rook Ceph</h3> <p>Installing Rook using <a href="https://helm.sh/">Helm</a> is pretty straightforward. First, you need to grab the helm repository and install the Rook-Ceph operator. <a href="https://rook.io/docs/rook/v1.13/Helm-Charts/operator-chart/#configuration">The default values are pretty solid</a>, so you might not need to specify a <code class="language-plaintext highlighter-rouge">values.yaml</code> file.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>helm repo add rook-release https://charts.rook.io/release
helm repo update
helm <span class="nb">install</span> <span class="nt">--create-namespace</span> <span class="nt">--namespace</span> rook-ceph rook-ceph rook-release/rook-ceph <span class="nt">-f</span> values.yaml
</code></pre></div></div> <p>After that, we need to install the Rook Ceph cluster. Again, <a href="https://rook.io/docs/rook/v1.13/Helm-Charts/ceph-cluster-chart/#configuration">the default values are very sane</a>, and they create a Ceph object bucket <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/">storage class</a> called <code class="language-plaintext highlighter-rouge">ceph-bucket</code>, which we will later use to create an <code class="language-plaintext highlighter-rouge">ObjectBucketClaim</code>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>helm <span class="nb">install</span> <span class="nt">--namespace</span> rook-ceph rook-ceph-cluster <span class="se">\</span>
    <span class="nt">--set</span> <span class="nv">operatorNamespace</span><span class="o">=</span>rook-ceph rook-release/rook-ceph-cluster <span class="nt">-f</span> cluster-values.yaml
</code></pre></div></div> <p>After a while, Rook should be ready. To check Ceph health, you can use the <a href="https://rook.io/docs/rook/v1.13/Troubleshooting/ceph-toolbox/">Rook toolbox</a> or <a href="https://rook.io/docs/rook/v1.13/Troubleshooting/kubectl-plugin/">the Rook kubectl plugin</a>:</p> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>kubectl rook-ceph ceph status
<span class="go">Info: running 'ceph' command with args: [status]
  cluster:
    id:     89bc2270-5e82-4833-99df-2d4338b0b81a
    health: HEALTH_OK

  services:
    mon: 3 daemons, quorum a,b,c (age 5d)
    mgr: a(active, since 5d), standbys: b
    mds: 1/1 daemons up, 1 hot standby
    osd: 3 osds: 3 up (since 5d), 3 in (since 5d)
    rgw: 1 daemon active (1 hosts, 1 zones)

  data:
    volumes: 1/1 healthy
    pools:   12 pools, 169 pgs
    objects: 436 objects, 668 KiB
    usage:   1.5 GiB used, 89 GiB / 90 GiB avail
    pgs:     169 active+clean

  io:
    client:   33 KiB/s rd, 0 B/s wr, 33 op/s rd, 21 op/s wr
</span></code></pre></div></div> <h3 id="knative-serving">Knative Serving</h3> <p>There are many ways to install Knative <a href="https://knative.dev/docs/install/">as described in the docs</a>. The easiest in my opinion is to use <a href="https://knative.dev/docs/install/operator/knative-with-operators/">the Knative Operator</a>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> https://github.com/knative/operator/releases/download/knative-v1.12.2/operator.yaml
</code></pre></div></div> <p>To install Knative Serving, create the following yaml file specifying the KnativeServing custom resource:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># serving.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Namespace</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">knative-serving</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">operator.knative.dev/v1beta1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">KnativeServing</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">knative-serving</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">knative-serving</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">ingress</span><span class="pi">:</span>
    <span class="na">kourier</span><span class="pi">:</span>
      <span class="na">enabled</span><span class="pi">:</span> <span class="kc">true</span>
  <span class="na">services</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">kourier</span>
      <span class="c1"># Override annotations for the kourier service</span>
      <span class="na">annotations</span><span class="pi">:</span>
        <span class="c1"># A provider-specific annotation is necessary to access the functions using a load balancer</span>
        <span class="c1"># For Hetzner it is:</span>
        <span class="na">load-balancer.hetzner.cloud/name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">my-load-balancer"</span>
  <span class="na">config</span><span class="pi">:</span>
    <span class="na">autoscaler</span><span class="pi">:</span>
      <span class="c1"># Retaining the function for longer keeps the machine learning model warm</span>
      <span class="c1"># and helps with debugging</span>
      <span class="na">scale-to-zero-pod-retention-period</span><span class="pi">:</span> <span class="s2">"</span><span class="s">20s"</span>
    <span class="na">domain</span><span class="pi">:</span>
      <span class="c1"># Replace with your domain if you would like to expose the service</span>
      <span class="s2">"</span><span class="s">your-subdomain.example.com"</span><span class="err">:</span> <span class="s2">"</span><span class="s">"</span>
    <span class="na">network</span><span class="pi">:</span>
      <span class="na">ingress-class</span><span class="pi">:</span> <span class="s2">"</span><span class="s">kourier.ingress.networking.knative.dev"</span>
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> serving.yaml
</code></pre></div></div> <h3 id="knative-eventing">Knative Eventing</h3> <p>We also need to install knative Eventing to handle bucket notifications. This is done by creating a <code class="language-plaintext highlighter-rouge">KnativeEventing</code> custom resource. <a href="https://github.com/knative-extensions/eventing-ceph">The Ceph bucket notifications event source</a> is enabled from the same resource.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># eventing.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Namespace</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">knative-eventing</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">operator.knative.dev/v1beta1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">KnativeEventing</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">knative-eventing</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">knative-eventing</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">source</span><span class="pi">:</span>
    <span class="na">ceph</span><span class="pi">:</span>
      <span class="na">enabled</span><span class="pi">:</span> <span class="kc">true</span>
</code></pre></div></div> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>kubectl apply <span class="nt">-f</span> eventing.yaml
<span class="gp">$</span><span class="w"> </span>kubectl get deployment <span class="nt">-n</span> knative-eventing
<span class="go">NAME                    READY   UP-TO-DATE   AVAILABLE   AGE
ceph-controller         1/1     1            1           44s
ceph-webhook            1/1     1            1           43s
eventing-controller     1/1     1            1           3m5s
eventing-webhook        1/1     1            1           3m5s
imc-controller          1/1     1            1           3m1s
imc-dispatcher          1/1     1            1           3m
mt-broker-controller    1/1     1            1           2m58s
mt-broker-filter        1/1     1            1           2m59s
mt-broker-ingress       1/1     1            1           2m59s
pingsource-mt-adapter   0/0     0            0           3m5s
</span><span class="gp">$</span><span class="w"> </span>kubectl get KnativeEventing knative-eventing <span class="nt">-n</span> knative-eventing
<span class="go">NAME               VERSION   READY   REASON
knative-eventing   1.12.0    True
</span></code></pre></div></div> <h2 id="tools-">Tools 🧰</h2> <h3 id="knative-func">Knative Func</h3> <p>Knative <code class="language-plaintext highlighter-rouge">func</code> is a CLI that makes it easy for users to create and deploy functions as Knative Services. You can <a href="https://knative.dev/docs/functions/install-func/">install it</a> using Homebrew:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew tap knative-extensions/kn-plugins
brew <span class="nb">install </span>func
</code></pre></div></div> <p>Or from GitHub releases:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wget <span class="nt">-O</span> func https://github.com/knative/func/releases/download/knative-v1.12.0/func_<span class="si">$(</span>go <span class="nb">env </span>GOOS<span class="si">)</span>_<span class="si">$(</span>go <span class="nb">env </span>GOARCH<span class="si">)</span>
<span class="nb">chmod</span> +x func
<span class="nb">sudo mv </span>func /usr/local/bin
func version
</code></pre></div></div> <h3 id="s5cmd">s5cmd</h3> <p>We also need a tool to interact with Ceph’s S3 API, like <code class="language-plaintext highlighter-rouge">s5cmd</code>. It could be installed from <a href="https://github.com/peak/s5cmd/releases">GitHub releases</a> or using Homebrew:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew <span class="nb">install </span>peak/tap/s5cmd
</code></pre></div></div> <h2 id="creating-the-buckets-">Creating the Buckets 🪣</h2> <p>For our application we need two buckets. One to store the ML model’s inputs and another to store the outputs. Rook handles bucket creation and access through a custom resource called <a href="https://rook.io/docs/rook/v1.13/Storage-Configuration/Object-Storage-RGW/ceph-object-bucket-claim/"><code class="language-plaintext highlighter-rouge">ObjectBucketClaim</code>, (OBC)</a>. This in turn creates a bucket with an auto-generated name in Ceph, and a <code class="language-plaintext highlighter-rouge">ConfigMap</code> and a <code class="language-plaintext highlighter-rouge">Secret</code> with the information needed to access the bucket.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># obc.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">objectbucket.io/v1alpha1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">ObjectBucketClaim</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">knative-ml-inputs</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">generateBucketName</span><span class="pi">:</span> <span class="s">ml-inputs</span>
  <span class="na">storageClassName</span><span class="pi">:</span> <span class="s">ceph-bucket</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">objectbucket.io/v1alpha1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">ObjectBucketClaim</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">knative-ml-outputs</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">generateBucketName</span><span class="pi">:</span> <span class="s">ml-outputs</span>
  <span class="na">storageClassName</span><span class="pi">:</span> <span class="s">ceph-bucket</span>
</code></pre></div></div> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>kubectl apply <span class="nt">-f</span> obc.yaml
<span class="gp">$</span><span class="w"> </span>kubectl describe objectbucketclaims/knative-ml-inputs
<span class="go">Name:         knative-ml-inputs
Namespace:    default
</span><span class="c">...
</span><span class="go">Spec:
  Bucket Name:           ml-inputs-9f6e8659-7bb5-4534-8579-9930ac8236dd
  Generate Bucket Name:  ml-inputs
  Object Bucket Name:    obc-default-knative-ml-inputs
  Storage Class Name:    ceph-bucket
Status:
  Phase:  Bound
</span><span class="gp">$</span><span class="w"> </span>kubectl describe configmap/knative-ml-inputs
<span class="go">Name:         knative-ml-inputs
Namespace:    default
Labels:       bucket-provisioner=rook-ceph.ceph.rook.io-bucket
</span><span class="gp">Annotations:  &lt;none&gt;</span><span class="w">
</span><span class="go">
Data
====
BUCKET_HOST:
----
rook-ceph-rgw-ceph-objectstore.rook-ceph.svc
BUCKET_NAME:
----
ml-inputs-882f3043-3fc8-43b8-8171-cb7872434197
BUCKET_PORT:
----
80
</span><span class="c">...
</span><span class="gp">$</span><span class="w"> </span>kubectl describe secret/knative-ml-inputs
<span class="go">Name:         knative-ml-inputs
Namespace:    default
Labels:       bucket-provisioner=rook-ceph.ceph.rook.io-bucket
</span><span class="gp">Annotations:  &lt;none&gt;</span><span class="w">
</span><span class="go">
Type:  Opaque

Data
====
AWS_ACCESS_KEY_ID:      20 bytes
AWS_SECRET_ACCESS_KEY:  40 bytes
</span></code></pre></div></div> <h2 id="the-code-">The Code! 🧑‍💻</h2> <p>We will use a Knative function to load and execute the Machine Learning model. <a href="https://huggingface.co/timbrooks/instruct-pix2pix">The model’s repository on Hugging Face</a> recommends using the <a href="https://huggingface.co/docs/diffusers/index">diffusers</a> Python library and has some sample code. We just need to add access to the Ceph buckets we created earlier and use CloudEvents to get the object’s name.</p> <p>Using <code class="language-plaintext highlighter-rouge">func</code>, we create a Python function.</p> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>kn func create <span class="nt">-l</span> python ml
<span class="go">Created python function in /path/to/ml
</span><span class="gp">$</span><span class="w"> </span>tree ml <span class="nt">-a</span>
<span class="go">ml
├── app.sh
├── .func
├── .funcignore
├── func.py
├── func.yaml
├── .gitignore
├── Procfile
├── README.md
├── requirements.txt
└── test_func.py
</span></code></pre></div></div> <p>Then, we need to add the <code class="language-plaintext highlighter-rouge">ConfigMaps</code> and <code class="language-plaintext highlighter-rouge">Secrets</code> for the two buckets as environment variables. This can be done by running <code class="language-plaintext highlighter-rouge">func config env add</code> and following the prompts, but it is less tedious to add the following directly to <code class="language-plaintext highlighter-rouge">ml/func.yaml</code></p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">run</span><span class="pi">:</span>
  <span class="na">envs</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">INPUTS_BUCKET_HOST</span>
      <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">INPUTS_BUCKET_PORT</span>
      <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">INPUTS_BUCKET_NAME</span>
      <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">INPUTS_ACCESS_KEY_ID</span>
      <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">INPUTS_SECRET_ACCESS_KEY</span>
      <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">OUTPUTS_BUCKET_HOST</span>
      <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">OUTPUTS_BUCKET_PORT</span>
      <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">OUTPUTS_BUCKET_NAME</span>
      <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">OUTPUTS_ACCESS_KEY_ID</span>
      <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">OUTPUTS_SECRET_ACCESS_KEY</span>
      <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>
</code></pre></div></div> <p>All the information needed to access the buckets is now stored in environment variables. We can now fill in the code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ml/func.py
</span><span class="kn">import</span> <span class="n">os</span>
<span class="kn">import</span> <span class="n">io</span>
<span class="kn">import</span> <span class="n">pathlib</span>
<span class="kn">from</span> <span class="n">parliament</span> <span class="kn">import</span> <span class="n">Context</span>
<span class="kn">import</span> <span class="n">boto3</span>
<span class="kn">import</span> <span class="n">PIL</span>
<span class="kn">from</span> <span class="n">diffusers</span> <span class="kn">import</span> <span class="n">StableDiffusionInstructPix2PixPipeline</span><span class="p">,</span> <span class="n">EulerAncestralDiscreteScheduler</span>

<span class="c1"># Access to the inputs bucket
</span><span class="n">in_endpoint</span> <span class="o">=</span> <span class="sh">"</span><span class="s">http://</span><span class="sh">"</span> <span class="o">+</span> <span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="sh">"</span><span class="s">INPUTS_BUCKET_HOST</span><span class="sh">"</span><span class="p">]</span>
<span class="n">in_bucket</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="sh">"</span><span class="s">INPUTS_BUCKET_NAME</span><span class="sh">"</span><span class="p">]</span>
<span class="n">in_s3</span> <span class="o">=</span> <span class="n">boto3</span><span class="p">.</span><span class="nf">client</span><span class="p">(</span><span class="sh">'</span><span class="s">s3</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">endpoint_url</span><span class="o">=</span><span class="n">in_endpoint</span><span class="p">,</span>
    <span class="n">aws_access_key_id</span><span class="o">=</span><span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="sh">"</span><span class="s">INPUTS_ACCESS_KEY_ID</span><span class="sh">"</span><span class="p">],</span>
    <span class="n">aws_secret_access_key</span><span class="o">=</span><span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="sh">"</span><span class="s">INPUTS_SECRET_ACCESS_KEY</span><span class="sh">"</span><span class="p">])</span>

<span class="c1"># Access to the outputs bucket
</span><span class="n">out_endpoint</span> <span class="o">=</span> <span class="sh">"</span><span class="s">http://</span><span class="sh">"</span> <span class="o">+</span> <span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="sh">"</span><span class="s">OUTPUTS_BUCKET_HOST</span><span class="sh">"</span><span class="p">]</span>
<span class="n">out_bucket</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="sh">"</span><span class="s">OUTPUTS_BUCKET_NAME</span><span class="sh">"</span><span class="p">]</span>
<span class="n">out_s3</span> <span class="o">=</span> <span class="n">boto3</span><span class="p">.</span><span class="nf">client</span><span class="p">(</span><span class="sh">'</span><span class="s">s3</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">endpoint_url</span><span class="o">=</span><span class="n">in_endpoint</span><span class="p">,</span>
    <span class="n">aws_access_key_id</span><span class="o">=</span><span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="sh">"</span><span class="s">OUTPUTS_ACCESS_KEY_ID</span><span class="sh">"</span><span class="p">],</span>
    <span class="n">aws_secret_access_key</span><span class="o">=</span><span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="sh">"</span><span class="s">OUTPUTS_SECRET_ACCESS_KEY</span><span class="sh">"</span><span class="p">])</span>

<span class="c1"># Load the model and keep in memory for subsequent runs
</span><span class="n">model_id</span> <span class="o">=</span> <span class="sh">"</span><span class="s">timbrooks/instruct-pix2pix</span><span class="sh">"</span>
<span class="n">pipe</span> <span class="o">=</span> <span class="n">StableDiffusionInstructPix2PixPipeline</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="n">model_id</span><span class="p">,</span> <span class="n">safety_checker</span><span class="o">=</span><span class="bp">None</span><span class="p">)</span>
<span class="n">pipe</span><span class="p">.</span><span class="nf">to</span><span class="p">(</span><span class="sh">"</span><span class="s">cpu</span><span class="sh">"</span><span class="p">)</span> <span class="c1"># Change to cuda if you have a gpu
</span><span class="n">pipe</span><span class="p">.</span><span class="n">scheduler</span> <span class="o">=</span> <span class="n">EulerAncestralDiscreteScheduler</span><span class="p">.</span><span class="nf">from_config</span><span class="p">(</span><span class="n">pipe</span><span class="p">.</span><span class="n">scheduler</span><span class="p">.</span><span class="n">config</span><span class="p">)</span>

<span class="n">prompt</span> <span class="o">=</span> <span class="sh">"</span><span class="s">turn it into a cartoon</span><span class="sh">"</span>

<span class="k">def</span> <span class="nf">read_input_image</span><span class="p">(</span><span class="n">key</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">PIL</span><span class="p">.</span><span class="n">Image</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Reads the image from the inputs bucket
    </span><span class="sh">"""</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Read input image</span><span class="sh">"</span><span class="p">,</span> <span class="n">flush</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
    <span class="n">file_byte_string</span> <span class="o">=</span> <span class="n">in_s3</span><span class="p">.</span><span class="nf">get_object</span><span class="p">(</span><span class="n">Bucket</span><span class="o">=</span><span class="n">in_bucket</span><span class="p">,</span> <span class="n">Key</span><span class="o">=</span><span class="n">key</span><span class="p">)[</span><span class="sh">"</span><span class="s">Body</span><span class="sh">"</span><span class="p">].</span><span class="nf">read</span><span class="p">()</span>
    <span class="n">image</span> <span class="o">=</span> <span class="n">PIL</span><span class="p">.</span><span class="n">Image</span><span class="p">.</span><span class="nf">open</span><span class="p">(</span><span class="n">io</span><span class="p">.</span><span class="nc">BytesIO</span><span class="p">(</span><span class="n">file_byte_string</span><span class="p">))</span>
    <span class="n">image</span> <span class="o">=</span> <span class="n">PIL</span><span class="p">.</span><span class="n">ImageOps</span><span class="p">.</span><span class="nf">exif_transpose</span><span class="p">(</span><span class="n">image</span><span class="p">)</span>
    <span class="n">image</span> <span class="o">=</span> <span class="n">image</span><span class="p">.</span><span class="nf">convert</span><span class="p">(</span><span class="sh">"</span><span class="s">RGB</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Input image size is </span><span class="sh">"</span> <span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="n">image</span><span class="p">.</span><span class="n">size</span><span class="p">),</span> <span class="n">flush</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">image</span>

<span class="k">def</span> <span class="nf">write_output_image</span><span class="p">(</span><span class="n">image</span><span class="p">:</span> <span class="n">PIL</span><span class="p">.</span><span class="n">Image</span><span class="p">,</span> <span class="n">key</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Writes the image to the output bucket
    </span><span class="sh">"""</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Write output image</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Output image size </span><span class="sh">"</span> <span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="n">image</span><span class="p">.</span><span class="n">size</span><span class="p">),</span> <span class="n">flush</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
    <span class="c1"># Save the image to an in-memory file
</span>    <span class="nb">file</span> <span class="o">=</span> <span class="n">io</span><span class="p">.</span><span class="nc">BytesIO</span><span class="p">()</span>
    <span class="nb">file</span><span class="p">.</span><span class="n">name</span> <span class="o">=</span> <span class="n">pathlib</span><span class="p">.</span><span class="nc">Path</span><span class="p">(</span><span class="n">key</span><span class="p">).</span><span class="n">name</span> <span class="c1"># Lets pillow figure out the file format
</span>    <span class="n">image</span><span class="p">.</span><span class="nf">save</span><span class="p">(</span><span class="nb">file</span><span class="p">)</span>
    <span class="nb">file</span><span class="p">.</span><span class="nf">seek</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

    <span class="c1"># Upload image to s3
</span>    <span class="n">out_s3</span><span class="p">.</span><span class="nf">upload_fileobj</span><span class="p">(</span><span class="nb">file</span><span class="p">,</span> <span class="n">out_bucket</span><span class="p">,</span> <span class="n">key</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">File is uploaded</span><span class="sh">"</span><span class="p">,</span> <span class="n">flush</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">main</span><span class="p">(</span><span class="n">context</span><span class="p">:</span> <span class="n">Context</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Called when uploading an image to in_bucket.
    Transforms the image using the prompt and uploads the result using out_bucket
    </span><span class="sh">"""</span>
    <span class="k">if</span> <span class="n">context</span><span class="p">.</span><span class="n">cloud_event</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
        <span class="k">return</span> <span class="sh">"</span><span class="s">A cloud event is required</span><span class="sh">"</span><span class="p">,</span> <span class="mi">400</span>

    <span class="n">event_attributes</span> <span class="o">=</span> <span class="n">context</span><span class="p">.</span><span class="n">cloud_event</span><span class="p">.</span><span class="nf">get_attributes</span><span class="p">()</span>
    <span class="n">key</span> <span class="o">=</span> <span class="n">event_attributes</span><span class="p">[</span><span class="sh">'</span><span class="s">subject</span><span class="sh">'</span><span class="p">]</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Key is </span><span class="sh">"</span> <span class="o">+</span> <span class="n">key</span><span class="p">)</span>

    <span class="n">image</span> <span class="o">=</span> <span class="nf">read_input_image</span><span class="p">(</span><span class="n">key</span><span class="p">)</span>
    <span class="n">result_images</span> <span class="o">=</span> <span class="nf">pipe</span><span class="p">(</span><span class="n">prompt</span><span class="p">,</span> <span class="n">image</span><span class="o">=</span><span class="n">image</span><span class="p">,</span> <span class="n">num_inference_steps</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span> <span class="n">image_guidance_scale</span><span class="o">=</span><span class="mi">1</span><span class="p">).</span><span class="n">images</span>
    <span class="nf">write_output_image</span><span class="p">(</span><span class="n">result_images</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">key</span><span class="p">)</span>
    <span class="k">return</span> <span class="sh">"</span><span class="s">Done</span><span class="sh">"</span><span class="p">,</span> <span class="mi">200</span>
</code></pre></div></div> <h2 id="building-and-deploying-️">Building and Deploying 🛠️</h2> <p>Let’s build and deploy this function. Replace <code class="language-plaintext highlighter-rouge">docker.io/&lt;your_username&gt;</code> with your registry.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>ml
kn func build <span class="nt">--registry</span> docker.io/&lt;your_username&gt;
kn func deploy
</code></pre></div></div> <h2 id="bucket-notifications-">Bucket Notifications 🔔</h2> <p>We would like to notify our function of uploaded objects and process them in a queue. Knative Eventing handles incoming events through an event source, which can be passed on to a broker to trigger a function. See the <a href="#diagram">figure above</a>.</p> <p>Let’s create a broker,</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># broker.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">eventing.knative.dev/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Broker</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">annotations</span><span class="pi">:</span>
    <span class="na">eventing.knative.dev/broker.class</span><span class="pi">:</span> <span class="s">MTChannelBasedBroker</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">ml-broker</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">delivery</span><span class="pi">:</span>
    <span class="na">backoffDelay</span><span class="pi">:</span> <span class="s">P10M</span> <span class="c1"># 10 minutes to allow for long execution</span>
</code></pre></div></div> <p>A trigger that calls the ml function,</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># trigger.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">eventing.knative.dev/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Trigger</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">ml-trigger</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">broker</span><span class="pi">:</span> <span class="s">ml-broker</span>
  <span class="na">subscriber</span><span class="pi">:</span>
    <span class="na">ref</span><span class="pi">:</span>
      <span class="na">apiVersion</span><span class="pi">:</span> <span class="s">serving.knative.dev/v1</span>
      <span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
      <span class="na">name</span><span class="pi">:</span> <span class="s">ml</span>
</code></pre></div></div> <p>And a Knative Event Source</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ceph-source.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">sources.knative.dev/v1alpha1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">CephSource</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">ml-ceph-source</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">default</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">port</span><span class="pi">:</span> <span class="s2">"</span><span class="s">8888"</span>
  <span class="na">sink</span><span class="pi">:</span>
    <span class="na">ref</span><span class="pi">:</span>
      <span class="na">apiVersion</span><span class="pi">:</span> <span class="s">eventing.knative.dev/v1</span>
      <span class="na">kind</span><span class="pi">:</span> <span class="s">Broker</span>
      <span class="na">name</span><span class="pi">:</span> <span class="s">default</span>
<span class="nn">---</span>
<span class="c1"># This will be called by Ceph notifications.</span>
<span class="c1"># The address of this service will be http://ml-ceph-source-svc.default.svc:80</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">ml-ceph-source-svc</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">default</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">eventing.knative.dev/sourceName</span><span class="pi">:</span> <span class="s">ml-ceph-source</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">80</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="m">8888</span>
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> broker.yaml
kubectl apply <span class="nt">-f</span> ceph-source.yaml
kubectl apply <span class="nt">-f</span> trigger.yaml
</code></pre></div></div> <p>Rook Ceph supports the creation of <a href="https://rook.io/docs/rook/v1.13/Storage-Configuration/Object-Storage-RGW/ceph-object-bucket-notifications/">bucket notifications</a> through the custom resources <code class="language-plaintext highlighter-rouge">CephBucketNotification</code> (which buckets will process notifications) and <code class="language-plaintext highlighter-rouge">CephBucketTopic</code> (where the notifications will go). We will direct the bucket notifications to the service we created earlier.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># notifications.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">ceph.rook.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">CephBucketTopic</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">ml-topic</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">objectStoreName</span><span class="pi">:</span> <span class="s">ceph-objectstore</span>
  <span class="na">objectStoreNamespace</span><span class="pi">:</span> <span class="s">rook-ceph</span>
  <span class="na">endpoint</span><span class="pi">:</span>
    <span class="na">http</span><span class="pi">:</span>
      <span class="c1"># This is the service uri from before</span>
      <span class="na">uri</span><span class="pi">:</span> <span class="s">http://ml-ceph-source-svc.default.svc:80</span>
      <span class="na">disableVerifySSL</span><span class="pi">:</span> <span class="kc">true</span>
      <span class="na">sendCloudEvents</span><span class="pi">:</span> <span class="kc">true</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">ceph.rook.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">CephBucketNotification</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">ml-notification</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">topic</span><span class="pi">:</span> <span class="s">ml-topic</span>
  <span class="na">events</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">s3:ObjectCreated:*</span>
</code></pre></div></div> <p>We also need to add a label to the <code class="language-plaintext highlighter-rouge">ObjectBucketClaim</code> of the input bucket to apply the notifications to it.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> notifications.yaml
kubectl patch obc/knative-ml-inputs <span class="nt">--type</span> merge <span class="se">\</span>
    <span class="nt">-p</span> <span class="s1">'{"metadata": {"labels": {"bucket-notification-ml-notification":"ml-notification"}}}'</span>
</code></pre></div></div> <h2 id="lets-run-this-">Let’s Run This! 🚀</h2> <p>Now we’re ready to roll! Well, not quite. We need to access the bucket from our local machine first. We could expose the bucket using an ingress like Nginx, or we could simply use a port-forward.</p> <details> <summary>(Optional) External access using an nginx ingress</summary> We are using cert-manager for the TLS certificates: <figure class="highlight"><pre><code class="language-yaml" data-lang="yaml"><span class="c1"># issuers.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">cert-manager.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Issuer</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">letsencrypt-prod</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">rook-ceph</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">acme</span><span class="pi">:</span>
    <span class="c1"># The ACME server URL</span>
    <span class="na">server</span><span class="pi">:</span> <span class="s">https://acme-v02.api.letsencrypt.org/directory</span>
    <span class="c1"># Email address used for ACME registration</span>
    <span class="na">email</span><span class="pi">:</span> <span class="s">admin@example.com</span>
    <span class="c1"># Name of a secret used to store the ACME account private key</span>
    <span class="na">privateKeySecretRef</span><span class="pi">:</span>
      <span class="na">name</span><span class="pi">:</span> <span class="s">letsencrypt-prod</span>
    <span class="c1"># Enable the HTTP-01 challenge provider</span>
    <span class="na">solvers</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">http01</span><span class="pi">:</span>
          <span class="na">ingress</span><span class="pi">:</span>
            <span class="na">ingressClassName</span><span class="pi">:</span> <span class="s">nginx</span></code></pre></figure> <figure class="highlight"><pre><code class="language-bash" data-lang="bash">helm repo add jetstack https://charts.jetstack.io
helm repo update

helm <span class="nb">install</span> <span class="se">\</span>
  cert-manager jetstack/cert-manager <span class="se">\</span>
  <span class="nt">--namespace</span> cert-manager <span class="se">\</span>
  <span class="nt">--create-namespace</span> <span class="se">\</span>
  <span class="nt">--version</span> v1.13.1 <span class="se">\</span>
  <span class="nt">--set</span> <span class="nv">installCRDs</span><span class="o">=</span><span class="nb">true

</span>kubectl apply <span class="nt">-f</span> issuers.yaml</code></pre></figure> As well as nginx for ingress: <figure class="highlight"><pre><code class="language-yaml" data-lang="yaml"><span class="c1"># nginx-values.yaml</span>
<span class="na">controller</span><span class="pi">:</span>
<span class="na">config</span><span class="pi">:</span>
  <span class="na">entries</span><span class="pi">:</span>
    <span class="na">use-forwarded-headers</span><span class="pi">:</span> <span class="s2">"</span><span class="s">true"</span>
    <span class="na">compute-full-forwarded-for</span><span class="pi">:</span> <span class="s2">"</span><span class="s">true"</span>
    <span class="na">use-proxy-protocol</span><span class="pi">:</span> <span class="s2">"</span><span class="s">true"</span>
<span class="na">service</span><span class="pi">:</span>
  <span class="na">annotations</span><span class="pi">:</span>
    <span class="c1"># This annotation is provicer-specific</span>
    <span class="na">load-balancer.hetzner.cloud/name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">your-lb"</span></code></pre></figure> <figure class="highlight"><pre><code class="language-bash" data-lang="bash">helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
helm repo update
helm <span class="nt">-n</span> ingress-nginx <span class="nb">install </span>ingress-nginx ingress-nginx/ingress-nginx <span class="nt">--create-namespace</span> <span class="nt">-f</span> deploy/nginx-values.yaml</code></pre></figure> Then we create an ingress: <figure class="highlight"><pre><code class="language-yaml" data-lang="yaml"><span class="c1"># ingress.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">networking.k8s.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Ingress</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">rook-ceph-rgw-external</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">rook-ceph</span>
  <span class="na">annotations</span><span class="pi">:</span>
    <span class="na">cert-manager.io/issuer</span><span class="pi">:</span> <span class="s">letsencrypt-prod</span>
    <span class="na">nginx.ingress.kubernetes.io/use-regex</span><span class="pi">:</span> <span class="s2">"</span><span class="s">true"</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">ingressClassName</span><span class="pi">:</span> <span class="s2">"</span><span class="s">nginx"</span>
  <span class="na">tls</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">hosts</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="s">your-subdomain.example.com</span>
      <span class="na">secretName</span><span class="pi">:</span> <span class="s">rook-ceph-rgw-external</span>
  <span class="na">rules</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">host</span><span class="pi">:</span> <span class="s">your-subdomain.example.com</span>
      <span class="na">http</span><span class="pi">:</span>
        <span class="na">paths</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="na">path</span><span class="pi">:</span> <span class="s">/</span>
            <span class="na">pathType</span><span class="pi">:</span> <span class="s">Prefix</span>
            <span class="na">backend</span><span class="pi">:</span>
              <span class="na">service</span><span class="pi">:</span>
                <span class="na">name</span><span class="pi">:</span> <span class="s">rook-ceph-rgw-ceph-objectstore</span>
                <span class="na">port</span><span class="pi">:</span>
                  <span class="na">number</span><span class="pi">:</span> <span class="s">80</span></code></pre></figure> <figure class="highlight"><pre><code class="language-bash" data-lang="bash">kubectl apply <span class="nt">-f</span> deploy/ingress.yaml</code></pre></figure> </details> <details> <summary>(Optional) External access using a port-forward</summary> You need to run this command in another terminal: <figure class="highlight"><pre><code class="language-console" data-lang="console"><span class="gp">$</span><span class="w"> </span>kubectl port-forward <span class="nt">-n</span> rook-ceph service/rook-ceph-rgw-ceph-objectstore 8888:80
<span class="gp">Forwarding from 127.0.0.1:8888 -&gt;</span><span class="w"> </span>8080
<span class="gp">Forwarding from [::1]:8888 -&gt;</span><span class="w"> </span>8080</code></pre></figure> </details> <p>Ok, now we’re ready for real. Let’s set up the credentials:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">S3_ENDPOINT_URL</span><span class="o">=</span>https://your-subdomain.example.com
<span class="c"># or if using port-forward</span>
<span class="c"># export S3_ENDPOINT_URL=http://localhost:8888</span>

<span class="nb">export </span><span class="nv">INPUTS_BUCKET_NAME</span><span class="o">=</span><span class="si">$(</span>kubectl <span class="nt">-n</span> default get cm knative-ml-inputs <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.data.BUCKET_NAME}'</span><span class="si">)</span>
<span class="nb">export </span><span class="nv">INPUTS_ACCESS_KEY_ID</span><span class="o">=</span><span class="si">$(</span>kubectl <span class="nt">-n</span> default get secret knative-ml-inputs <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.data.AWS_ACCESS_KEY_ID}'</span> | <span class="nb">base64</span> <span class="nt">--decode</span><span class="si">)</span>
<span class="nb">export </span><span class="nv">INPUTS_SECRET_ACCESS_KEY</span><span class="o">=</span><span class="si">$(</span>kubectl <span class="nt">-n</span> default get secret knative-ml-inputs <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.data.AWS_SECRET_ACCESS_KEY}'</span> | <span class="nb">base64</span> <span class="nt">--decode</span><span class="si">)</span>

<span class="nb">export </span><span class="nv">OUTPUTS_BUCKET_NAME</span><span class="o">=</span><span class="si">$(</span>kubectl <span class="nt">-n</span> default get cm knative-ml-outputs <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.data.BUCKET_NAME}'</span><span class="si">)</span>
<span class="nb">export </span><span class="nv">OUTPUTS_ACCESS_KEY_ID</span><span class="o">=</span><span class="si">$(</span>kubectl <span class="nt">-n</span> default get secret knative-ml-outputs <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.data.AWS_ACCESS_KEY_ID}'</span> | <span class="nb">base64</span> <span class="nt">--decode</span><span class="si">)</span>
<span class="nb">export </span><span class="nv">OUTPUTS_SECRET_ACCESS_KEY</span><span class="o">=</span><span class="si">$(</span>kubectl <span class="nt">-n</span> default get secret knative-ml-outputs <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.data.AWS_SECRET_ACCESS_KEY}'</span> | <span class="nb">base64</span> <span class="nt">--decode</span><span class="si">)</span>

<span class="nb">mkdir</span> ~/.aws
<span class="nb">cat</span> <span class="o">&gt;</span> ~/.aws/credentials <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
[inputs]
aws_access_key_id = </span><span class="k">${</span><span class="nv">INPUTS_ACCESS_KEY_ID</span><span class="k">}</span><span class="sh">
aws_secret_access_key = </span><span class="k">${</span><span class="nv">INPUTS_SECRET_ACCESS_KEY</span><span class="k">}</span><span class="sh">

[outputs]
aws_access_key_id = </span><span class="k">${</span><span class="nv">OUTPUTS_ACCESS_KEY_ID</span><span class="k">}</span><span class="sh">
aws_secret_access_key = </span><span class="k">${</span><span class="nv">OUTPUTS_SECRET_ACCESS_KEY</span><span class="k">}</span><span class="sh">
</span><span class="no">EOF
</span></code></pre></div></div> <p>To test our pipeline, we will use an image at the path <code class="language-plaintext highlighter-rouge">input-imgs/pexels-justin-shaifer.jpg</code>.</p> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>s5cmd <span class="nt">--profile</span> inputs <span class="nb">cp </span>input-imgs/pexels-justin-shaifer.jpg s3://<span class="nv">$INPUTS_BUCKET_NAME</span>
<span class="go">cp input-imgs/pexels-justin-shaifer.jpg s3://ml-inputs-882f3043-3fc8-43b8-8171-cb7872434197/pexels-justin-shaifer.jpg
</span><span class="gp">$</span><span class="w"> </span>s5cmd <span class="nt">--profile</span> inputs <span class="nb">ls </span>s3://<span class="nv">$INPUTS_BUCKET_NAME</span>
<span class="go">2024/02/28 07:00:47             29146  pexels-justin-shaifer.jpg
</span></code></pre></div></div> <p>We can track the progress of the function using <code class="language-plaintext highlighter-rouge">kubectl logs -f</code>:</p> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>kubectl get pods
<span class="go">NAME                                                              READY   STATUS        RESTARTS   AGE
cephsource-my-ceph-source-416fe9a2-82f8-4f2a-9e73-7dd1545fg7qxc   1/1     Running       0          6d13h
knative-operator-5945688b59-lh2tq                                 1/1     Running       0          6d13h
ml-00001-deployment-9c5cbfb95-cn6hl                               2/2     Running       0          7s
operator-webhook-8557b8ddff-2kzp9                                 1/1     Running       0          6d13h
</span><span class="gp">$</span><span class="w"> </span>kubectl logs ml-00001-deployment-9c5cbfb95-cn6hl
<span class="c">...
</span><span class="go">Loading pipeline components...: 100%|██████████| 6/6 [00:00&lt;00:00,  9.89it/s]0, 495MB/s]
Key is pexels-justin-shaifer.jpg
Read input image
Input image size is (640, 427)
100%|██████████| 10/10 [00:54&lt;00:00,  5.45s/it]
Write output image
Output image size (640, 424)
File is uploaded
</span></code></pre></div></div> <p>When the function is done processing the image, you can find it in the output bucket:</p> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>s5cmd <span class="nt">--profile</span> outputs <span class="nb">ls </span>s3://<span class="nv">$OUTPUTS_BUCKET_NAME</span>
<span class="go">2024/02/28 07:05:16             13750  pexels-justin-shaifer.jpg
</span><span class="gp">$</span><span class="w"> </span>s5cmd <span class="nt">--profile</span> outputs <span class="nb">cp </span>s3://<span class="nv">$OUTPUTS_BUCKET_NAME</span>/pexels-justin-shaifer.jpg <span class="nb">.</span>
<span class="go">cp s3://ml-outputs-a560800a-9fa8-4f13-bc0d-3e787dbfbc7b/pexels-justin-shaifer.jpg pexels-justin-shaifer.jpg
</span></code></pre></div></div> <div class="row mt-3 justify-content-center"> <div class="col-sm-6 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2024-03-05-ceph-ai/input-480.webp 480w,/assets/img/2024-03-05-ceph-ai/input-800.webp 800w,/assets/img/2024-03-05-ceph-ai/input-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2024-03-05-ceph-ai/input.jpg" class="img-fluid z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm-6 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2024-03-05-ceph-ai/output-480.webp 480w,/assets/img/2024-03-05-ceph-ai/output-800.webp 800w,/assets/img/2024-03-05-ceph-ai/output-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2024-03-05-ceph-ai/output.jpg" class="img-fluid z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Left: Photo by Justin Shaifer from Pexels, Right: The output image using the prompt "turn it into a cartoon" </div> <h2 id="rook-ceph-is-a-perfect-match-for-your-ai-needs">Rook Ceph is a Perfect Match for Your AI Needs</h2> <p>As we saw in this tutorial, Rook Ceph effortlessly fits your Machine Learning storage needs. You can run them both on the same cluster or use a separate cluster to host your Ceph S3 buckets.</p>]]></content><author><name></name></author><category term="ceph"/><category term="knative"/><category term="ai"/><summary type="html"><![CDATA[Running a pre-trained Machine Learning model as a serverless function made easy with Knative and Ceph object storage]]></summary></entry><entry><title type="html">Serverless Storage with Knative and Ceph</title><link href="https://zalsader.com/blog/2024/ceph-knative/" rel="alternate" type="text/html" title="Serverless Storage with Knative and Ceph"/><published>2024-01-29T21:01:00+00:00</published><updated>2024-01-29T21:01:00+00:00</updated><id>https://zalsader.com/blog/2024/ceph-knative</id><content type="html" xml:base="https://zalsader.com/blog/2024/ceph-knative/"><![CDATA[<p>This post was originally written while I worked at <a href="https://koor.tech">Koor Technologies</a>. Since that website is no longer online, I am republishing <a href="https://web.archive.org/web/20240416174014/https://blog.koor.tech/blog/2024/serverless-storage-knative-ceph/">that article</a> here.</p> <div class="row mt-3 justify-content-center"> <div class="col-sm-6 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2024-01-24-ceph-knative/serverless-storage-knative-ceph-480.webp 480w,/assets/img/2024-01-24-ceph-knative/serverless-storage-knative-ceph-800.webp 800w,/assets/img/2024-01-24-ceph-knative/serverless-storage-knative-ceph-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2024-01-24-ceph-knative/serverless-storage-knative-ceph.jpg" class="img-fluid z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>You’ve probably heard some buzz about serverless computing. Well, the “serverless” part is a metaphor, your functions still run on servers, just like how cloud computing generally involves no water vapor (I’m not aware of any steam-powered data centers). The innovation in serverless computing is allowing developers to write and run short functions (aka lambdas) without knowing a lot about the underlying infrastructure. The function is run by the service provider and developers are usually billed based on the resources used during execution. This model of cloud computing is also known as Function-as-a-Service (FaaS).</p> <p>Serverless functions are commonly used to develop responsively scalable APIs for websites without the overhead of setting up an infrastructure. They are also used to process external events from services like Twilio, Stripe and Salesforce. Some serverless functions use large machine-learning models stored on external storage. Data processing is another example of using serverless functions, where data like images, videos or logs could go through a pipeline with multiple stages of processing to get the desired result. These pipelines often use a message queue, a PubSub system or some storage between stages. Using storage is preferred when the data exchanged between stages is large.</p> <p>This blog series explores different options for using storage in <a href="https://knative.dev/">Knative</a>, a popular open-source serverless platform.</p> <h2 id="knative-serverless-functions-on-your-servers">Knative: Serverless Functions on your Servers</h2> <p>Similar to how you can bring cloud infrastructure to your own data center using <a href="https://www.openstack.org/">OpenStack</a> and <a href="https://kubernetes.io/">Kubernetes</a>, you can bring serverless computing to your own servers. This migration back to local cloud services could be motivated by cost or by business needs to keep data and computation partially or fully separated from the public cloud.</p> <p>One popular platform to run serverless workloads is <a href="https://knative.dev/">Knative</a>, which runs on Kubernetes and is used in <a href="https://cloud.google.com/blog/products/serverless/knative-based-cloud-run-services-are-ga">Google Cloud Run</a>. Knative is built on Kubernetes, which allows it to be platform-agnostic and easily scalable.</p> <p>Knative consists of two main components: Serving and Eventing. Knative Serving manages the serverless workload as containers in Kubernetes and handles routing and scaling. Knative Eventing is a collection of APIs that allow using event-driven architecture with serverless applications.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2024-01-24-ceph-knative/knative_flowchart_graphic.svg" sizes="95vw"/> <img src="/assets/img/2024-01-24-ceph-knative/knative_flowchart_graphic.svg" class="img-fluid z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The two components of Knative: Serving and Eventing, from the Knative homepage<br/> used under <a href="https://github.com/knative/docs/blob/main/LICENSE-docs" target="_blank">CC BY 4.0</a> and recolored </div> <p>Many applications of serverless architecture, like image and video processing, require access to storage to store inputs, outputs or intermediate objects. When using AWS Lambda as a serverless platform, <a href="https://aws.amazon.com/blogs/compute/choosing-between-aws-lambda-data-storage-options-in-web-apps/">the general wisdom</a> is to use AWS S3 or AWS EFS. Since Knative is based on Kubernetes, it is natural to think about managing storage in Kubernetes too - away from the costs associated with the public cloud. This is where Rook Ceph shines.</p> <p>This first part of the series walks through installing Knative on a Kubernetes Cluster and attaching Ceph storage to functions. It uses an example of a file processing pipeline with a producer and a consumer.</p> <h2 id="prerequisites">Prerequisites</h2> <p>You need to have a working Kubernetes cluster that meets the minimum requirements of both <a href="https://rook.io/docs/rook/v1.13/Getting-Started/Prerequisites/prerequisites/">Rook</a> and <a href="https://knative.dev/docs/install/operator/knative-with-operators/#prerequisites">Knative</a>. This tutorial uses Rook v1.13.0 and Knative v1.12.0 . This is a summary of the requirements for a production cluster:</p> <ul> <li>Kubernetes v1.26 or newer.</li> <li>At least three nodes, each having at least 2 CPUs, 4 GB of memory, and 20 GB of disk storage.</li> <li><a href="https://rook.io/docs/rook/v1.13/Getting-Started/Prerequisites/prerequisites/#ceph-prerequisites">Raw devices</a> / partitions / logical volumes</li> </ul> <p>We ran this using our demo cluster, which we set up using <a href="https://www.terraform.io/">Terraform</a> and <a href="https://github.com/kubermatic/kubeone">Kubeone</a> on <a href="https://www.hetzner.com/cloud">Hetzner Cloud</a>. All the code is <a href="https://github.com/koor-tech/demo-gitops/tree/b12e89953ea71beed97f1f8b182df6f7fd096a1d/exp/knative-basic">available on GitHub</a>.</p> <h3 id="installing-knative">Installing Knative</h3> <p>There are many ways to install Knative as <a href="https://knative.dev/docs/install/">described in the docs</a>. The easiest in my opinion is to use the Knative Operator:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> https://github.com/knative/operator/releases/download/knative-v1.12.2/operator.yaml
</code></pre></div></div> <p>To install Knative Serving, create the following yaml file specifying the <code class="language-plaintext highlighter-rouge">KnativeServing</code> custom resource:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># serving.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Namespace</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">knative-serving</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">operator.knative.dev/v1beta1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">KnativeServing</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">knative-serving</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">knative-serving</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">ingress</span><span class="pi">:</span>
    <span class="na">kourier</span><span class="pi">:</span>
      <span class="na">enabled</span><span class="pi">:</span> <span class="kc">true</span>
  <span class="na">services</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">kourier</span>
      <span class="c1"># Override annotations for the kourier service</span>
      <span class="na">annotations</span><span class="pi">:</span>
        <span class="c1"># A provider-specific annotation is necessary to access the functions using a load balancer</span>
        <span class="c1"># For Hetzner it is:</span>
        <span class="na">load-balancer.hetzner.cloud/name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">my-load-balancer"</span>
  <span class="na">config</span><span class="pi">:</span>
    <span class="na">domain</span><span class="pi">:</span>
      <span class="s2">"</span><span class="s">your-subdomain.example.com"</span><span class="err">:</span> <span class="s2">"</span><span class="s">"</span> <span class="c1"># Replace with your domain if you would like to expose the service</span>
    <span class="na">network</span><span class="pi">:</span>
      <span class="na">ingress-class</span><span class="pi">:</span> <span class="s2">"</span><span class="s">kourier.ingress.networking.knative.dev"</span>
    <span class="na">features</span><span class="pi">:</span>
      <span class="na">kubernetes.podspec-persistent-volume-claim</span><span class="pi">:</span> <span class="s2">"</span><span class="s">enabled"</span>
      <span class="na">kubernetes.podspec-persistent-volume-write</span><span class="pi">:</span> <span class="s2">"</span><span class="s">enabled"</span>
      <span class="na">kubernetes.podspec-securitycontext</span><span class="pi">:</span> <span class="s2">"</span><span class="s">enabled"</span>
</code></pre></div></div> <p>The last three lines are required because accessing persistent volume claims from Knative functions is not enabled by default, and you need to enable that using <a href="https://knative.dev/docs/serving/configuration/feature-flags/#kubernetes-persistentvolumeclaim-pvc">a feature flag</a>. We also need the <a href="https://knative.dev/docs/serving/configuration/feature-flags/#kubernetes-security-context">security context</a> feature flag to change volume permissions, <a href="#building-and-deploying-%EF%B8%8F">see below</a>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> serving.yaml
</code></pre></div></div> <h3 id="installing-knative-func">Installing Knative Func</h3> <p>Knative <code class="language-plaintext highlighter-rouge">func</code> is a CLI that makes it easy for users to create and deploy functions as Knative Services. You can <a href="https://knative.dev/docs/functions/install-func/">install it</a> using Homebrew or from GitHub releases.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wget <span class="nt">-O</span> func https://github.com/knative/func/releases/download/knative-v1.12.0/func_<span class="si">$(</span>go <span class="nb">env </span>GOOS<span class="si">)</span>_<span class="si">$(</span>go <span class="nb">env </span>GOARCH<span class="si">)</span>
<span class="nb">chmod</span> +x func
<span class="nb">sudo mv </span>func /usr/local/bin
func version
</code></pre></div></div> <h3 id="installing-rook-ceph">Installing Rook Ceph</h3> <p>Installing Rook using <a href="https://helm.sh/">Helm</a> is pretty straightforward. First, you need to grab the helm repository and install the Rook-Ceph operator. <a href="https://rook.io/docs/rook/v1.13/Helm-Charts/operator-chart/#configuration">The default values are pretty solid</a>, so you might not need to specify a <code class="language-plaintext highlighter-rouge">values.yaml</code> file.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>helm repo add rook-release https://charts.rook.io/release
helm repo update
helm <span class="nb">install</span> <span class="nt">--create-namespace</span> <span class="nt">--namespace</span> rook-ceph rook-ceph rook-release/rook-ceph <span class="nt">-f</span> values.yaml
</code></pre></div></div> <p>After that, we need to install the Rook Ceph cluster. Again, <a href="https://rook.io/docs/rook/v1.13/Helm-Charts/ceph-cluster-chart/#configuration">the default values are very sane</a>, and they create a CephFS <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/">storage class</a> called <code class="language-plaintext highlighter-rouge">ceph-filesystem</code>, which is what we will use to create a <code class="language-plaintext highlighter-rouge">PersistentVolumeClaim</code> later.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>helm <span class="nb">install</span> <span class="nt">--namespace</span> rook-ceph rook-ceph-cluster <span class="se">\</span>
   <span class="nt">--set</span> <span class="nv">operatorNamespace</span><span class="o">=</span>rook-ceph rook-release/rook-ceph-cluster <span class="nt">-f</span> cluster-values.yaml
</code></pre></div></div> <p>After a while, Rook should be ready. To check Ceph health, you can use the <a href="https://rook.io/docs/rook/v1.13/Troubleshooting/ceph-toolbox/">Rook toolbox</a> or the <a href="https://rook.io/docs/rook/v1.13/Troubleshooting/kubectl-plugin/">Rook kubectl plugin</a>:</p> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>kubectl <span class="nb">exec</span> <span class="nt">-n</span> rook-ceph <span class="nt">-it</span> deploy/rook-ceph-tools <span class="nt">--</span> ceph status
<span class="go">  cluster:
    id:     6a7e98c9-521f-4e2a-a0c3-f9433910c07f
    health: HEALTH_OK

  services:
    mon: 3 daemons, quorum a,b,c (age 10d)
    mgr: b(active, since 116s), standbys: a
    mds: 1/1 daemons up, 1 hot standby
    osd: 3 osds: 3 up (since 4w), 3 in (since 8w)
    rgw: 1 daemon active (1 hosts, 1 zones)

  data:
    volumes: 1/1 healthy
    pools:   12 pools, 185 pgs
    objects: 447 objects, 149 MiB
    usage:   1.7 GiB used, 88 GiB / 90 GiB avail
    pgs:     185 active+clean

  io:
    client:   853 B/s rd, 1 op/s rd, 0 op/s wr
</span></code></pre></div></div> <h2 id="lets-write-some-code-">Let’s write some code! 🧑‍💻</h2> <p>To simulate a data pipeline, we will use two functions that share a CephFS volume. The producer function creates a file with random data in storage, and the consumer function chooses a file, checks its md5 then deletes it. We also have a function that lists the files in storage to track progress. A real data pipeline could consist of multiple stages of more sophisticated processing, like machine learning, video filters, or data aggregators.</p> <p>First, we create the <code class="language-plaintext highlighter-rouge">PersistentVolumeClaim</code>:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># pvc.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">PersistentVolumeClaim</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">knative-pc-cephfs</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">accessModes</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">ReadWriteMany</span>
  <span class="na">resources</span><span class="pi">:</span>
    <span class="na">requests</span><span class="pi">:</span>
      <span class="na">storage</span><span class="pi">:</span> <span class="s">11Gi</span>
  <span class="na">storageClassName</span><span class="pi">:</span> <span class="s">ceph-filesystem</span> <span class="c1"># This is the CephFS storage class we created earlier</span>
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> pvc.yaml
</code></pre></div></div> <p>Then, we create the functions. We will use Golang for our example, but you can choose any of <a href="https://github.com/knative/func/blob/main/docs/language-packs/language-pack-contract.md#built-in-language-packs">the supported languages</a>.</p> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>func create <span class="nt">-l</span> go producer
<span class="go">Created go function in /path/to/producer
</span><span class="gp">$</span><span class="w"> </span>tree producer <span class="nt">-a</span>
<span class="go">producer
├── .func
├── .funcignore
├── func.yaml
├── .gitignore
├── go.mod
├── handle.go
├── handle_test.go
└── README.md
</span><span class="gp">$</span><span class="w"> </span>func create <span class="nt">-l</span> go consumer
<span class="go">Created go function in /path/to/consumer
</span><span class="gp">$</span><span class="w"> </span>func create <span class="nt">-l</span> go list
<span class="go">Created go function in /path/to/list
</span></code></pre></div></div> <p>We can now add the PVC we created earlier to each of the functions. Use this command then follow the prompts to add <code class="language-plaintext highlighter-rouge">knative-pc-cephfs</code> as a volume. The mount path is where your function will be able to access the volume, in this case, it is <code class="language-plaintext highlighter-rouge">/files</code>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>producer
func config volumes add
</code></pre></div></div> <p>Doing this adds the following lines to your <code class="language-plaintext highlighter-rouge">func.yaml</code>:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># producer/func.yaml</span>
<span class="na">run</span><span class="pi">:</span>
  <span class="na">volumes</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">presistentVolumeClaim</span><span class="pi">:</span>
        <span class="na">claimName</span><span class="pi">:</span> <span class="s">knative-pc-cephfs</span>
      <span class="na">path</span><span class="pi">:</span> <span class="s">/files</span>
</code></pre></div></div> <p>We can now access the CephFS storage like any directory. This is the producer function for example:</p> <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// producer/handle.go</span>
<span class="k">package</span> <span class="n">function</span>

<span class="k">import</span> <span class="p">(</span>
	<span class="s">"context"</span>
	<span class="s">"crypto/md5"</span>
	<span class="n">cryptoRand</span> <span class="s">"crypto/rand"</span>
	<span class="s">"fmt"</span>
	<span class="s">"math/rand"</span>
	<span class="s">"net/http"</span>
	<span class="s">"os"</span>
<span class="p">)</span>

<span class="k">const</span> <span class="n">FILES_DIR</span> <span class="o">=</span> <span class="s">"/files"</span>

<span class="c">// Handle an HTTP Request.</span>
<span class="k">func</span> <span class="n">Handle</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">res</span> <span class="n">http</span><span class="o">.</span><span class="n">ResponseWriter</span><span class="p">,</span> <span class="n">req</span> <span class="o">*</span><span class="n">http</span><span class="o">.</span><span class="n">Request</span><span class="p">)</span> <span class="p">{</span>
	<span class="c">// Generates a file between 5 and 15 MB</span>
	<span class="n">fileSize</span> <span class="o">:=</span> <span class="p">(</span><span class="n">rand</span><span class="o">.</span><span class="n">Intn</span><span class="p">(</span><span class="m">10</span><span class="p">)</span> <span class="o">+</span> <span class="m">5</span><span class="p">)</span> <span class="o">*</span> <span class="m">1000</span><span class="n">_000</span>
	<span class="n">fmt</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"Generated file size will be %d"</span><span class="p">,</span> <span class="n">fileSize</span><span class="p">)</span>

	<span class="n">fmt</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="s">"Generating random bytes"</span><span class="p">)</span>
	<span class="n">contents</span> <span class="o">:=</span> <span class="nb">make</span><span class="p">([]</span><span class="kt">byte</span><span class="p">,</span> <span class="n">fileSize</span><span class="p">)</span>
	<span class="n">generatedSize</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">cryptoRand</span><span class="o">.</span><span class="n">Read</span><span class="p">(</span><span class="n">contents</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">res</span><span class="o">.</span><span class="n">WriteHeader</span><span class="p">(</span><span class="n">http</span><span class="o">.</span><span class="n">StatusInternalServerError</span><span class="p">)</span>
		<span class="n">fmt</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
		<span class="n">fmt</span><span class="o">.</span><span class="n">Fprint</span><span class="p">(</span><span class="n">res</span><span class="p">,</span> <span class="s">"Error generating random data"</span><span class="p">)</span>
		<span class="k">return</span>
	<span class="p">}</span>
	<span class="n">fmt</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"Generated %d random bytes"</span><span class="p">,</span> <span class="n">generatedSize</span><span class="p">)</span>

	<span class="n">md5sum</span> <span class="o">:=</span> <span class="n">md5</span><span class="o">.</span><span class="n">Sum</span><span class="p">(</span><span class="n">contents</span><span class="p">)</span>
	<span class="n">fmt</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"The md5 sum is %x</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">md5sum</span><span class="p">)</span>

	<span class="n">fileName</span> <span class="o">:=</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Sprintf</span><span class="p">(</span><span class="s">"%x.bin"</span><span class="p">,</span> <span class="n">md5sum</span><span class="p">)</span>
	<span class="n">filePath</span> <span class="o">:=</span> <span class="n">FILES_DIR</span> <span class="o">+</span> <span class="s">"/"</span> <span class="o">+</span> <span class="n">fileName</span>
	<span class="n">fmt</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"Writing file %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">fileName</span><span class="p">)</span>
	<span class="n">err</span> <span class="o">=</span> <span class="n">os</span><span class="o">.</span><span class="n">WriteFile</span><span class="p">(</span><span class="n">filePath</span><span class="p">,</span> <span class="n">contents</span><span class="p">,</span> <span class="m">0666</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">res</span><span class="o">.</span><span class="n">WriteHeader</span><span class="p">(</span><span class="n">http</span><span class="o">.</span><span class="n">StatusInternalServerError</span><span class="p">)</span>
		<span class="n">fmt</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
		<span class="n">fmt</span><span class="o">.</span><span class="n">Fprintf</span><span class="p">(</span><span class="n">res</span><span class="p">,</span> <span class="s">"Error writing to file %s"</span><span class="p">,</span> <span class="n">fileName</span><span class="p">)</span>
		<span class="k">return</span>
	<span class="p">}</span>
	<span class="n">fmt</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"Wrote file %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">fileName</span><span class="p">)</span>

	<span class="n">fmt</span><span class="o">.</span><span class="n">Fprintf</span><span class="p">(</span><span class="n">res</span><span class="p">,</span> <span class="s">"Produced file %s </span><span class="se">\n</span><span class="s">File size is %d </span><span class="se">\n</span><span class="s">The md5 sum is %x"</span><span class="p">,</span> <span class="n">fileName</span><span class="p">,</span> <span class="n">fileSize</span><span class="p">,</span> <span class="n">md5sum</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div> <p>You can find the full code for all the functions on <a href="https://github.com/koor-tech/demo-gitops/tree/b12e89953ea71beed97f1f8b182df6f7fd096a1d/exp/knative-basic">our GitHub repository</a>.</p> <h2 id="building-and-deploying-️">Building and Deploying 🛠️</h2> <p>Now we are ready to build and deploy these functions. Replace <code class="language-plaintext highlighter-rouge">docker.io/&lt;your_username&gt;</code> with your registry.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>producer
func build <span class="nt">--registry</span> docker.io/&lt;your_username&gt;
func deploy

<span class="c"># this is to fix permission issues</span>
kubectl patch services.serving/producer <span class="nt">--type</span> merge <span class="se">\</span>
    <span class="nt">-p</span> <span class="s1">'{"spec": {"template": {"spec": {"securityContext": {"fsGroup":1000}}}}}'</span>
</code></pre></div></div> <p>That last command is needed because of a mismatch in user permissions for the attached volume. <a href="https://github.com/knative/func/issues/2108">A GitHub issue</a> was raised to the knative team to address that. Deploy the other functions similarly, replacing <code class="language-plaintext highlighter-rouge">producer</code> in the last command with the name of the function.</p> <h2 id="lets-run-this-">Let’s Run This! 🚀</h2> <p>Now that the function is deployed, we can invoke it using func invoke. This is the result of invoking the producer function:</p> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>func invoke
<span class="go">Produced file 4f55c654d31edfe1362879a31c815076.bin
File size is 12000000
The md5 sum is 4f55c654d31edfe1362879a31c815076
</span></code></pre></div></div> <p>Alternatively, we can invoke a function using an http request to the url returned by <code class="language-plaintext highlighter-rouge">func deploy</code>. This uses the subdomain defined when <a href="#installing-knative">installing Knative Serving</a>. For example, this is the result of running the list and consumer functions:</p> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>curl http://list.default.knative-staging.koor.tech
<span class="go">Found 1 files
- 4f55c654d31edfe1362879a31c815076.bin
</span><span class="gp">$</span><span class="w"> </span>curl http://consumer.default.your-subdomain.example.com
<span class="go">Consumed file 4f55c654d31edfe1362879a31c815076.bin
File size is 12000000
The md5 sum is 4f55c654d31edfe1362879a31c815076
</span></code></pre></div></div> <p>When you’re done with a function, you can delete it using <code class="language-plaintext highlighter-rouge">func delete</code></p> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>func delete consumer
<span class="go">Removing Knative Service: consumer
Removing Knative Service 'consumer' and all dependent resources
</span></code></pre></div></div> <h2 id="rook-ceph-is-an-excellent-match-for-knative">Rook Ceph is an excellent match for Knative</h2> <p>As we’ve seen in this tutorial, setting up Knative with Rook Ceph as a storage provider is easy. Both tools use Kubernetes as a substrate which means you can save resources by running both of them on the same cluster. You can also use the <a href="https://rook.io/docs/rook/v1.13/CRDs/Cluster/external-cluster/">Ceph CSI driver</a> to connect to Ceph storage on a different cluster.</p> <p>Stay tuned for <a href="/blog/2024/ceph-ai/">the next part in this series</a> where we use the Ceph S3 API with Knative.</p>]]></content><author><name></name></author><category term="ceph"/><category term="knative"/><summary type="html"><![CDATA[Do you need to access storage in your Knative functions? Here’s how to do it with Rook Ceph]]></summary></entry><entry><title type="html">Welcome to my blog!</title><link href="https://zalsader.com/blog/2023/hello/" rel="alternate" type="text/html" title="Welcome to my blog!"/><published>2023-01-29T21:01:00+00:00</published><updated>2023-01-29T21:01:00+00:00</updated><id>https://zalsader.com/blog/2023/hello</id><content type="html" xml:base="https://zalsader.com/blog/2023/hello/"><![CDATA[<p>I recently updated my website to use <a href="https://github.com/alshedivat/al-folio">al-folio</a> Jekyll theme. It is simple to use and looks minimally beautiful. The native dark theme is a good bonus. I am hosting this on Github pages behind Cloudflare. I will keep playing around with this over the next while. The theme includes the ability to write blog posts, which I will probably use for random tech things or whatever projects I am working on. Still figuring things out as I go.</p>]]></content><author><name></name></author><category term="general"/><summary type="html"><![CDATA[I wonder what this will be about]]></summary></entry></feed>